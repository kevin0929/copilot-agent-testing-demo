# Environment Configuration for Secure API
# This file demonstrates how to properly configure sensitive credentials

# Database Configuration
DB_DRIVER=ODBC Driver 17 for SQL Server
DB_SERVER=localhost
DB_DATABASE=TestDB
DB_USERNAME=testuser
DB_PASSWORD=testpassword

# LDAP Configuration
LDAP_SERVER=ldap://localhost:389
LDAP_USER=testadmin
LDAP_PASSWORD=testldappassword
LDAP_BASE_DN=dc=company,dc=com

# API Configuration
API_KEY=your-secure-api-key-here
SECRET_KEY=your-secure-secret-key-here
ENCRYPTION_KEY=your-secure-encryption-key-here

# Admin Configuration
ADMIN_PASSWORD=your-secure-admin-password-here

# Backup URLs
BACKUP_URLS=http://localhost:8080/backup,http://localhost:8081/backup

# Instructions:
# 1. Copy this file to .env (add to .gitignore)
# 2. Replace all example values with actual secure credentials
# 3. Use a secrets management system in production (AWS Secrets Manager, HashiCorp Vault, etc.)
# 4. Never commit actual credentials to version control
